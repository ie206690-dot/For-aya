<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>For Ayat ❤️</title>

<style>
body {
    margin: 0;
    height: 100vh;
    overflow: hidden;
    background: linear-gradient(135deg, #ffe6f0, #fff0f5);
    font-family: Arial, sans-serif;
    text-align: center;
}

h1 {
    margin-top: 100px;
    font-size: 40px;
    color: #ff3366;
    animation: fadeIn 2s ease-in-out;
}

p {
    font-size: 22px;
    color: #444;
}

button {
    padding: 12px 25px;
    font-size: 18px;
    border: none;
    border-radius: 30px;
    cursor: pointer;
    background-color: #ff4d6d;
    color: white;
    margin-top: 30px;
    transition: 0.3s;
}

button:hover {
    transform: scale(1.1);
}

.heart {
    position: absolute;
    color: #ff4d6d;
    animation: float 6s linear infinite;
}

@keyframes float {
    from { transform: translateY(100vh); opacity: 1; }
    to { transform: translateY(-10vh); opacity: 0; }
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}
</style>
</head>

<body>

<h1>🌸 Ayat ❤️</h1>
<p>You make my world brighter every single day ✨</p>
<p>I’m so lucky to have you 💕</p>

<button onclick="showLove()">Click Me 💌</button>

<script>

function showLove() {
    document.body.innerHTML = `
    <div style="margin-top:150px; animation: fadeIn 2s;">
        <h1 style="color:#ff3366;">Ayat ❤️</h1>
        <h2>You are my favorite person in the world 🥹</h2>
        <p style="font-size:20px;">Forever & Always 💞</p>
    </div>
    `;
    createHearts(60);
}

function createHearts(num) {
    for (let i = 0; i < num; i++) {
        let heart = document.createElement("div");
        heart.classList.add("heart");
        heart.innerHTML = "❤️";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.fontSize = Math.random() * 20 + 20 + "px";
        heart.style.animationDuration = (Math.random() * 3 + 3) + "s";
        document.body.appendChild(heart);
    }
}

setInterval(() => {
    let heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "💖";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.fontSize = Math.random() * 15 + 15 + "px";
    heart.style.animationDuration = (Math.random() * 3 + 4) + "s";
    document.body.appendChild(heart);
}, 500);

</script>

</body>
</html>
